# Static fallback for clean product URLs when Cloudflare Functions are not used.
# If Cloudflare Pages Functions are deployed, /functions/product/[id].js handles /product/:id.
/product/*  /product.html  200
/product    /product.html  200
